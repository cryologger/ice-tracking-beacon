  /*
    // SPI Flash Configuration
    SPI1.begin();
    if (flash.begin()) {
      DEBUG_PRINTLN("SPI flash memory detected.");
      flash.powerDown();  // Puts device in low power state. Will only respond to powerUp()
      //flash.powerUp();    // Wakes chip from low power state
    }
    else {
      DEBUG_PRINTLN("SPI flash memory not detected! Please check wiring. Halting.");
      //while (1);
    }
  */
