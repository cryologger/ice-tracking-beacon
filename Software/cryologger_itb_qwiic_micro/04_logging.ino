  /*
    // SPI Flash Configuration
    SPI1.begin();
    if (flash.begin()) {
      SERIAL_PORT.println(F("SPI flash memory detected."));
      flash.powerDown();  // Puts device in low power state. Will only respond to powerUp()
      //flash.powerUp();    // Wakes chip from low power state
    }
    else {
      SERIAL_PORT.println(F("SPI flash memory not detected! Please check wiring. Halting."));
      //while (1);
    }
  */
